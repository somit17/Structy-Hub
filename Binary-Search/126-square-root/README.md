# square root

Write a function that takes in a nonnegative integer n as input. The function should return the square root of n
rounded down to the nearest integer.

You may not use built-in methods like `math.sqrt()` or `**` that trivialize this problem.

Your solution should have a time complexity of O(log(n)).

#### test_00

```python
square_root(16) # -> 4
```

#### test_01

```python
square_root(81) # -> 9
```

#### test_02

```python
square_root(9) # -> 3
```

#### test_03

```python
square_root(1) # -> 1
```

#### test_04

```python
square_root(64) # -> 8
```

#### test_05

```python
square_root(67) # -> 8
```

#### test_06

```python
square_root(196) # ->14
```

#### test_07

```python
square_root(204) # ->14
```
